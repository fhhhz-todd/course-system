<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.course.mapper.MessageMapper">

    <select id="selectByUserId" resultType="com.example.course.entity.Message">
        SELECT * FROM messages WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="selectByUserId" resultType="com.example.course.entity.Message">
        SELECT * FROM messages WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>
    
    <select id="selectAllUserIds" resultType="java.lang.String">
        SELECT id FROM "USER"
    </select>
    
    <select id="selectUserIdsByRole" resultType="java.lang.String">
        SELECT id FROM "USER" WHERE role = #{role}
    </select>
    
    <select id="searchMessages" resultType="com.example.course.entity.Message">
        SELECT * FROM messages 
        WHERE user_id = #{userId}
        <if test="keyword != null and keyword != ''">
            AND (title LIKE CONCAT('%', #{keyword}, '%') OR content LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{size}
    </select>

</mapper>